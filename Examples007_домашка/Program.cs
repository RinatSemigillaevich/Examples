double Count = 0;
double Distanse = 10000;
double FirstFrendSpeed = 1;
double SecondFrendsSpeed = 2;
double DogSpeed = 3;
int Frend = 2;

while(Distanse > 10)
{
    if(frend =1);
    double Time = Distanse / (SecondFrendsSpeed + DogSpeed);
    else(frend = 2);
    double Time = Distanse/(FirstFrendSpeed + DogSpeed);

 double Distanse = Distanse - (FirstFrendSpeed + SecondFrendsSpeed) * Time;
 
 int Count ++;
}

 Console.Write("собака пробежит ");
 Console.WriteLine("count");